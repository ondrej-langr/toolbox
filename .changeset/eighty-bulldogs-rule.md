---
'@ondrej-langr/susumu': minor
---

POSSIBLE BREAKING: added possibility to add custom projects while preserving already created ones with migration. When there is a pending data migration it will prevent user from creating new entries. Each project gets its own color and can be renamed. Lastly each row gets its own timer that stops when new item comes after it.
