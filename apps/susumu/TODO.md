What is currently missing

- cannot change logged items in previous days
  - It should be possible to edit existing, add or remove new items for selected day with current time.
  - when adding items to previous days there should be prompt to confirm such update before adding it
- New feature request: closing notes for day
  - as a user i want to define closing notes as todo list for current day and display closing notes as todos for previous days. The closing notes definition should be rendered after the main content (where new items are being added) and closing notes for previous day before the logged tasks for today. When the task is done cross it and change the opacity. When there is no task from previous day then do not render it. Make it also possible to collapse the list of tasks from previous day
